export function Header() {
  return (
    <header className="hero">
      <span className="hero__eyebrow">Transcrição + conteúdo</span>
      <h1 className="hero__title">Transcritor NextLevel</h1>
      <p className="hero__description">
        Envie um arquivo, gere a transcrição e depois crie versões de conteúdo a partir do texto.
      </p>
    </header>
  );
}
